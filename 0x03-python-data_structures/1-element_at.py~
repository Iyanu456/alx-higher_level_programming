#!/usr/bin/python3
list = [1, 2, 3, 4, 5]
def element_at(my_list, idx):
    if idx < 0:
        return ("None")
    elif idx > (len(my_list) - 1):
        return ("None")
    else:
        return (my_list[idx])
print("Element at index 3 is {}".format(element_at(list, 5)))
